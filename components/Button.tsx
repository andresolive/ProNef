import { View, Text } from 'react-native';

interface CircleButtonProps {
  imgUrl: string;
  right: number;
  top: number;
}

export const CircleButton = ({ imgUrl, right, top }: CircleButtonProps) => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};

export const RectButton = () => {
  return (
    <View>
      <Text></Text>
    </View>
  );
};
